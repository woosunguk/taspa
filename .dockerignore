# 빌드 컨텍스트 축소 — 빌드에 불필요한 산출물/개발 파일 제외.
.git
.gitignore
.gradle
.kotlin
**/build
build
node_modules
e2e
docs
.omc
.claude
.idea
.vscode
*.iml
.DS_Store

# 시크릿/환경 파일은 절대 이미지에 넣지 않는다.
# ★`.env`·`*.env` 만으로는 부족하다 — `.env.prod` 는 둘 중 어느 패턴에도 걸리지 않는다
#   (앞은 정확히 .env, 뒤는 `.env` 로 **끝나는** 이름). 배포 매니페스트 디렉터리째 제외한다.
.env
*.env
.env.*
deploy
application-local.yml

# 문서(빌드 무관)
*.md
