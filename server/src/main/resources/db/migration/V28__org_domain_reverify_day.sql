-- 도메인 재검증 실패 카운터의 날짜 멱등성.
--   기존: 재검증 잡이 실행될 때마다 reverify_failures += 1 → 다중 인스턴스에서 인스턴스 수만큼 증가해
--   "3일 연속 실패 시 철회"라는 설계 의도가 하루 만에 소진된다(정상 도메인의 조기 철회 = 자동 가입 중단).
--   수정: 마지막으로 실패를 집계한 날짜를 기록하고, 같은 날 두 번째부터는 증가시키지 않는다.
--   이로써 카운터가 "연속 실행 횟수"가 아니라 의도대로 "연속 실패 일수"가 된다(수동 재실행에도 안전).
ALTER TABLE org_domains ADD COLUMN last_reverify_failure_on DATE;
