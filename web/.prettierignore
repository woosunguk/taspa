# 빌드 산출물·의존성
.next/
node_modules/
coverage/

# 운영 아티팩트(오케스트레이션 상태) — 우리가 쓰는 파일이 아니다
.omc/

# 문서는 손으로 줄을 맞춘다.
#
# ★prettier 에 맡기면 줄바꿈이 재배치돼 강조와 문단 리듬이 바뀐다. 이 저장소의 문서(CLAUDE.md·AGENTS.md·
# docs/**)는 "왜 이렇게 했는지"를 설명하는 문단이 많고, 그 줄바꿈 자체가 읽는 순서를 만든다.
# 포맷 게이트의 목적은 코드 diff 를 깨끗하게 유지하는 것이므로 문서는 대상이 아니다.
*.md
